# Local state
/.env
/.venv/
/.terraform/
terraform/.terraform/
terraform/*.tfstate*

# Homelab state (do not commit)
**/state.env

# Secrets
**/*.sops.*
**/*.age
**/*.key
**/*.token

# Editors
.vscode/
.idea/
.DS_Store

# Secrets and local state
*.sops.yaml.dec
**/secrets.yaml
**/secrets.yml
# Local-only files
.local/.homelab_repo_root
.homelab_repo_root
t.sh
# Keep the runs directory itself in the repo
!state/runs/.gitkeep
!state/logs/.gitkeep
# Ignore everything inside it (all subfolders and files)
state/runs/*
state/logs/*
