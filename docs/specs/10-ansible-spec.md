# Ansible configuration specification

Uses static or dynamic inventory, runs syntax/check in dry run, and executes playbooks in live mode. Updates latest.env with inventory path and step completion.
