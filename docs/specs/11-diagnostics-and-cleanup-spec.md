# Diagnostics and cleanup specification

Diagnostics reports gate status and last run info without mutation. Cleanup clears caches and logs with confirmations; never removes secrets without explicit warning.
