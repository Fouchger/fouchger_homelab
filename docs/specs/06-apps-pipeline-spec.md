# Apps pipeline specification

Supports install/uninstall via checklists, persists to state/selections.env, validates module presence and prerequisites, executes deterministically, and logs per app. Dry run produces a plan report.
